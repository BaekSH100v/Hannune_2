# Hannune login background asset

The production WebP is reconstructed during `npm run build` from the six base64 chunks in this folder by `scripts/build-login-bg.mjs`. This avoids binary truncation through the repository connector while preserving the full 1672×941 background image.
